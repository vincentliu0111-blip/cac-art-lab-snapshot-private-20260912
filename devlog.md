# CAC Art Lab 开发日志

记录项目的功能变更、运行结果和待办事项。早期进度于 2026-09-11 根据已有文件与运行记录整理；没有记录的耗时不补写。

## 2026-09-10 — 建立项目

建立 `cac-art-lab` 仓库，加入 `.gitattributes`、`.gitignore` 和初始 `README.md`。

## 2026-09-11 — 固定抽样与基础导出

完成读取 JSON、建立画作与画对索引、固定随机抽样及基础数据导出。原始数据放在 `Data/`，生成内容放在根目录下的 `web/`。

基础阶段修正了三个问题：循环改为遍历抽样后的 `selected`，区间统计使用 `q_A`，输出路径改为 `BASE.parent / "web"`。固定参数为 `seed=7`，默认及最终输出为 `N=60`。

## 2026-09-11 — 特征差异、缩略图与复现检查

完成内容：

- 计算全部 300 幅画的 28 个特征极差。
- 用 `feature_ranges()` 和 `top_differences()` 计算 A−B、归一化差值与每对画的 top 3 特征；处理零极差和同值名称排序。
- 在 `ReadData.py` 中接入特征差异及缩略图输出。
- 使用 Pillow 生成保持比例、长边不超过 400 像素的 RGB JPEG，采用 LANCZOS 和 `quality=88`。
- 整理运行说明、依赖版本、检查工具及实际验收记录。

检查过程：合成数据覆盖零极差、正负差值、同值名称排序和 `limit`。首对画产生 28 条差值；单张图片由 600×432 缩为 400×288。正式生成与检查在本地项目仓库完成。

正式验收时间为 2026-09-11 10:37:01–10:37:04（UTC−04:00）。按 N=60→40→60 的顺序，每个设置重新生成并检查两轮，共运行生成器 6 次、检查器 6 次。

| 阶段 | JSON 条数 | `0.4 <= q_A <= 0.6` 条数 | 引用图片数 | 检查结果 |
|---|---:|---:|---:|---|
| 第一次 N=60 | 60 | 5 | 106 | 首轮通过并建立指纹，第二轮复现通过 |
| N=40 | 40 | 3 | 75 | 首轮通过并建立指纹，第二轮复现通过 |
| 最终 N=60 | 60 | 5 | 106 | 两轮均通过，复现通过 |

`verification.txt` 由检查器实际运行生成，保留 N=60 三条与 N=40 一条复现通过记录。恢复 N=60 后已存在对应指纹，因此最后两轮都会追加通过记录。

- N=60 输出指纹：`6efdccdddc86cb4ddfc1e154c2785aebe2d61d076f773ede1497df3b0e61e859`。
- N=40 输出指纹：`fb423ff18fcfedf46c937f7e6991e6d35ac741debd8b2c145ad22c0646926cce`。

最终 JSON 有 60 条，每条包含 3 个 `top_features`；引用的 106 张 JPEG 均可完整解码，尺寸与比例通过检查。313 个原始数据文件（含 300 张原图和 `Data/README.md`）保持原样。N=40 阶段保留已有缩略图，统计的是当前 JSON 实际引用的图片数。

运行环境为 Python 3.9.6、Pillow 11.3.0。首次安装 Pillow 时出现域名解析失败，联网后安装成功；版本固定在 `requirements.txt`。

## 2026-09-11 — 整理项目说明

统一 README、开发日志和代码说明的表达。本次文字调整不改变数据处理或验收逻辑，原始数据与已生成的 JSON、图片保持不变。公开仓库以当前文件快照发布。

## 下一步

保存最终 N=60 的真实终端截图，完成网页界面、选择交互、结果页与部署。当前 M1 的通过记录不代表网页和赛事提交已经完成。
